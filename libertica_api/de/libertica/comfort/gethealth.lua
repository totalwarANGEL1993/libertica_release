--- Gibt die relative Gesundheit einer Entität zurück.

--- Gibt die relative Gesundheit einer Entität zurück.
--- @param _Entity any ID oder Skriptname
--- @return number Percentage Relative Gesundheit der Entität
function GetHealth(_Entity)
    return 0;
end
API.GetHealth = GetHealth;

